# GB 2762-2025 审计（二）：备注+检测方法 重点复核 + 此前期问题"应显示内容"转化

> 审计对象：`jianyu/data/gb2762/gb2762_2025.json`（12 表 / 299 行）
> 审计时间：2026-09-03（续）　方法：逐表比对 PDF 原文脚注字母+全文、逐表比对"检验方法"段、核对应用渲染逻辑（`pickMethod`+`footnotes`）
> **本轮仅出报告，未改动任何应用/数据文件。**

---

## 一句话结论

- **备注（脚注）**：发现 **2 处虚构脚注**（表1 生咖啡豆 `d`、表2 花生 `b`），应用目前会把这两条**标准里不存在的脚注显示出来**；另有 1 处脚注 e 措辞标点差异（含义相同）。其余所有脚注字母与全文均与 PDF 一致。
- **检测方法**：发现 **1 处规则缺失**（表2 漏了"包装饮用水→GB 8538"特殊规则），导致表2 的包装饮用水/饮用天然矿泉水**应用显示成 GB 5009.15（应为 GB 8538）**。其余 11 张表的检验方法规则均与 PDF 一致。
- 此前阶段 A 的确定性问题（螺旋藻数值错、孤儿挂载、重复行等）已逐条转成"现状→应显示内容"见第三部分。

---

## 一、备注（脚注）审核

### ❌ 错误 1：表1 铅 · 生咖啡豆及烘焙咖啡豆（共 2 行）显示了虚构脚注 `d`

| 项目 | 内容 |
|---|---|
| 现状（应用显示） | 脚注块出现一行：**「d 仅限生咖啡豆及烘焙咖啡豆」** |
| 应显示 | **无任何脚注**（GB 2762-2025 表1 根本没有 d 脚注；生咖啡豆只是单独成行、有自己的限量 0.5，不需要脚注） |
| PDF 真实脚注（表1 仅 a/b/c） | a 稻谷以糙米计。／ b 新鲜香辛料(如姜、葱、蒜等)应按对应的新鲜蔬菜(或新鲜水果)类别执行。／ c 液态婴幼儿配方食品根据8∶1的比例折算其限量。 |
| 数据错在哪 | ① 这两行 item 的 `note` 字段被误写成 `'d'`；② 表1 的 `footnotes` 字典里被人为塞进了一条 `'d': "仅限生咖啡豆及烘焙咖啡豆"`（标准里没有，属虚构） |
| 改法（暂未执行） | 删掉这两行 item 的 `note` 值（清空）；从表1 `footnotes` 列表中删除 label=`d` 的那条 |

> 这 2 行 food 分别为「坚果及籽类 › 生干坚果及籽类… › 生咖啡豆及烘焙咖啡豆」和「坚果及籽类 › 坚果及籽类制品 › 熟制坚果及籽类… › 生咖啡豆及烘焙咖啡豆」，两行都要清 `note`。

### ❌ 错误 2：表2 镉 · 花生（共 2 行）显示了虚构脚注 `b`

| 项目 | 内容 |
|---|---|
| 现状（应用显示） | 脚注块出现一行：**「b 仅限花生」** |
| 应显示 | **无任何脚注**（GB 2762-2025 表2 只有 a 脚注，没有 b；花生只是单独成行、限量 0.5，不需要脚注） |
| PDF 真实脚注（表2 仅 a） | a 稻谷以糙米计。 |
| 数据错在哪 | ① 这两行 item 的 `note` 被误写成 `'b'`；② 表2 的 `footnotes` 字典里被人为塞进 `'b': "仅限花生"`（标准里没有，属虚构） |
| 改法（暂未执行） | 删掉这两行 item 的 `note` 值（清空）；从表2 `footnotes` 列表中删除 label=`b` 的那条 |

### ⚠️ 轻微：表8 亚硝酸盐、硝酸盐 · 脚注 `e` 措辞与 PDF 差一个标点（含义相同，非必须改）

- 数据：`仅适用于乳基产品,不含豆类成分。`
- PDF：`仅适用于乳基产品(不含豆类成分)。`
- 仅逗号/括号差异，意思完全一样。若追求与国标逐字一致，把表8 `footnotes` 中 `e` 的 text 改成 PDF 原文即可。

### ✅ 已核对正确（备注部分无遗漏）

逐字母、逐字比对 12 张表全部脚注，除上述 2 处虚构 + 1 处标点外，**其余所有脚注（表1 a/b/c、表2 a、表3 a/b、表4 a/b、表5 a、表7 a、表8 a/b/c/d/e、表9 a、表11 a、表12 a）字母与全文均与 PDF 一致**，且数据里所有 `note`/`remark`/`main_remark`/`sub_remark` 引用的字母都能在对应表真实脚注中找到。表8 两条 N/A 行（婴幼儿辅助食品 L=—/—、婴幼儿配方食品 L=—/—）的 `im='—'` 也是正确的（无限量即无检验方法）。

---

## 二、检测方法（inspection_method）审核

### ❌ 错误 3：表2 镉 漏了"包装饮用水→GB 8538"特殊规则，导致 2 行水显示错方法

应用渲染逻辑是：`检测方法 = pickMethod(表级规则) || 该行的 inspection_method`。表级规则优先。表2 的规则目前**只有一条**「全部食品→GB 5009.15」，缺少国标明确写的"包装饮用水按 GB 8538"。

| 项目 | 内容 |
|---|---|
| 受影响行 | 表2 镉 ·「包装饮用水(饮用天然矿泉水除外)」限量 0.005；「饮用天然矿泉水」限量 0.003 |
| 现状（应用显示） | 这两行 检测方法 显示 **「按 GB 5009.15 规定的方法测定」** |
| 应显示 | **「按 GB 8538 规定的方法测定」**（GB 2762-2025 表2 检验方法原文："包装饮用水按 GB 8538 规定的方法测定，其他食品按 GB 5009.15 规定的方法测定。"） |
| 数据现状 | 这两行 item 的 `inspection_method` 本身已正确写成 `'GB 8538'`，但被表级规则（GB 5009.15）覆盖，所以显示错 |
| PDF 真实检验方法（表2） | 包装饮用水按 GB 8538；其他食品按 GB 5009.15 |
| 改法（暂未执行） | 在表2 `inspection_method_rules` 列表里，**在默认规则之前**加一条非默认规则：`{"apply":"包装饮用水","method":"按 GB 8538 规定的方法测定"}`。加完后 pickMethod 命中"包装饮用水"即返回 GB 8538，与 per-row 值一致 |

> 对比：表1/表3/表4/表8 都已正确写了水类的特殊规则（包装饮用水→GB8538 / 饮用天然矿泉水→GB8538 / 饮料类→GB8538），唯独表2 漏了。

### ✅ 已核对正确（检测方法部分无遗漏）

- 逐表比对 PDF"检验方法"段与数据 `inspection_method_rules`：**表1、表3、表4、表5、表6、表7、表8、表9、表10、表11、表12 全部 11 张表的规则与 PDF 一致**（含表8 用"饮料类"命中、表3/表4 用"饮用天然矿泉水/包装饮用水"命中均正确）。
- 全部 299 行 per-row `inspection_method` 与规则解析出的 GB 编号逐一比对，**除表2 上述 2 行水外无第二处冲突**（表8 的 2 行水 per-row=GB8538 与规则"饮料类→GB8538"一致，正确）。

### ℹ️ 数据完整性提示（非显示错误，可选补）

以下 5 行 per-row `inspection_method` 为空，但**应用靠表级规则已正确兜底显示**，不影响用户看到的内容，仅属数据冗余字段空缺，可酌情补全以便导出/复用：

| 表 | 食品 | 应用实际显示（由规则兜底） | 建议补全的 per-row 值 |
|---|---|---|---|
| 表1 铅 | 包装饮用水 | GB 8538 | `GB 8538` |
| 表4 砷 | 食用菌及其制品(松茸…木耳…银耳…除外) | GB 5009.11 | `GB 5009.11` |
| 表9 苯并[a]芘 | 稀奶油、奶油、无水奶油 | GB 5009.27 | `GB 5009.27` |
| 表9 苯并[a]芘 | 熏、烤水产品 | GB 5009.27 | `GB 5009.27` |
| 表9 苯并[a]芘 | 油脂及其制品 | GB 5009.27 | `GB 5009.27` |
| 表9 苯并[a]芘 | 熏、烧、烤肉类 | GB 5009.27 | `GB 5009.27` |

---

## 三、此前阶段 A 确定性问题 → 转"应显示的污染物限量内容"

（以下是您之前看到的那版审计里的确定项，按您要求转成"现状显示什么 → 应该显示什么"，方便您决定怎么改）

### C. 限量值硬错 1 处

| 表 | 食品 | 现状显示 | 应显示 | 字段改法 |
|---|---|---|---|---|
| 表1 铅 | 螺旋藻 | 限量 **0.5** mg/kg | 限量 **2.0（干重计）** mg/kg | item 螺旋藻：`limit_value` `'0.5'`→`'2.0'`，`modif` 设为 `'干重计'`。（同表"螺旋藻制品"已正确为 2.0，仅"螺旋藻"错） |

### A1. 分类树缺子节点（补树后这些行才能在分类树里正常显示）

| 表 | 食品 | 现状 | 应显示（补树后） | 字段改法 |
|---|---|---|---|---|
| 表1 铅 | 黄酒 | 挂在「酒类›发酵酒…」下，但树里"发酵酒"无子节点，点不开 | 树里"发酵酒"下应有「黄酒」节点，点击后显示 限量 0.5 | 在 A.1 树"发酵酒"下补「黄酒」节点 |
| 表1/表3 | 木耳及其制品、银耳及其制品 | 挂在「食用菌及其制品›食用菌制品›银耳」下，树里"食用菌制品"无"银耳/木耳"子节点 | 树里"食用菌制品"下应有「银耳」「木耳」节点 | 在 A.1 树"食用菌制品"下补「银耳」「木耳」 |
| 表11 | 水产动物油脂 | 挂在「油脂及其制品›动物油脂…›水产动物油脂」下，树里"动物油脂"无子节点 | 树里"动物油脂"下应有「水产动物油脂」节点 | 在 A.1 树"动物油脂"下补「水产动物油脂」 |

### A2. 命名未对齐（节点带"例如"注解，数据用裸名，挂不上）

| 表 | 食品(数据裸名) | 现状 | 应显示 | 字段改法 |
|---|---|---|---|---|
| 表2 镉 | 牛肝菌（及松茸等食用菌制品） | 数据 a1_l3='牛肝菌'，树节点是"牛肝菌[美味牛肝菌,兰茂牛肝菌,…]" | 二者统一后可正常显示 限量 1.0 | 统一命名：要么树节点去"[]"注解，要么数据 a1_l3 带注解 |
| 表2 镉 | 甲壳类(海蟹、虾蛄除外) | 数据 a1_l3='甲壳类'，树节点"甲壳类（例如：虾类、蟹类等）" | 统一后可显示 限量 2.0 | 同上，去掉"例如"注解或数据对齐 |
| 表7 铬 | 豆类 | 数据 a1_l3='豆类'，树节点"豆类（干豆、以干豆磨成的粉）" | 统一后可显示 限量 1.0 | 同上 |

### A3 + D. 无效/重复挂载路径清理

| 表 | 食品 | 现状 | 应显示 | 字段改法 |
|---|---|---|---|---|
| 表2 镉 | 畜禽肝脏及其制品 / 畜禽肾脏及其制品 | 除正确的「肉及肉制品›肉类（生鲜肉…）›畜禽内脏」外，还错误地多挂在「肉及肉制品›肉制品（…）›畜禽内脏」（该路径树里不存在） | 仅保留正确路径，删除无效路径 | 删掉这两行 item 里指向"肉制品›畜禽内脏"的挂载（保留"肉类›畜禽内脏"） |

### B. 完全重复行 6 组（食用菌集中，不影响显示，仅数据整洁）

| 表 | 食品 | 现状 | 应显示 | 字段改法 |
|---|---|---|---|---|
| 表2 镉 | 芹菜、黄花菜（限量0.2） | 2 行完全相同 | 保留 1 行 | 删 1 行 |
| 表2 镉 | 香菇及其制品（0.5） | 2 行 | 保留 1 行 | 删 1 行 |
| 表2 镉 | 羊肚菌、獐头菌、青头菌、鸡油菌、榛蘑及以上食用菌的制品（0.6） | 6 行 | 保留 1 行 | 删 5 行 |
| 表2 镉 | 松茸、牛肝菌、鸡枞、多汁乳菇及以上食用菌的制品（1.0） | 5 行 | 保留 1 行 | 删 4 行 |
| 表2 镉 | 松露、姬松茸及以上食用菌的制品（2.0） | 3 行 | 保留 1 行 | 删 2 行 |
| 表2 镉 | 木耳及其制品、银耳及其制品（0.5） | 2 行 | 保留 1 行 | 删 1 行 |

---

## 四、修改清单（汇总，本轮均未执行）

| 编号 | 类别 | 表·食品 | 核心改动 |
|---|---|---|---|
| 1 | 备注/虚构脚注 | 表1·生咖啡豆及烘焙咖啡豆(2行) | 清 `note`；删表1 footnotes 的 `d` |
| 2 | 备注/虚构脚注 | 表2·花生(2行) | 清 `note`；删表2 footnotes 的 `b` |
| 3 | 备注/措辞 | 表8·脚注 e | text 改 PDF 原文（标点，可选） |
| 4 | 检测方法/规则缺失 | 表2·包装饮用水、饮用天然矿泉水 | 表2 `inspection_method_rules` 加 包装饮用水→GB 8538 |
| 5 | 数值硬错 | 表1·螺旋藻 | limit 0.5→2.0(干重计) |
| 6 | 树缺节点 | 表1/表3·黄酒/银耳/木耳、表11·水产动物油脂 | A.1 树补 4 个子节点 |
| 7 | 命名对齐 | 表2·牛肝菌/甲壳类、表7·豆类 | 树节点或数据 a1_l3 统一 |
| 8 | 无效挂载 | 表2·畜禽肝/肾 | 删"肉制品›畜禽内脏"无效路径 |
| 9 | 去重 | 表2·食用菌 6 组 | 各保留 1 行 |

> 说明：编号 1–4 为本轮（备注+检测方法）新发现；编号 5–9 为阶段 A 既有确定项，此处仅做"应显示内容"转化，供您统一决策。
